// Edit content.json to modify site texts and image references.
// Images should be placed in the /public/images folder.
import contentData from './content.json';

export const footerInfoLinks = contentData.footerInfoLinks;
export const siteContent = contentData.siteContent;
